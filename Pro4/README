遍历每一个inode，如果其中非空，则处理对应文件。
在新的磁盘中从前往后分配空间。
使用递归的思想处理，例如处理2级间接索引会变为处理1级简介索引，直到变为0级，即直接索引时才会将旧的文件复制到新的磁盘中。
每次送入下一级处理前都要先分配下一级索引的地址，并写入这一级的块中。

./defrag <name of disk> 进行磁盘整理，生成 <name of disk-defrag> 新的磁盘。

./test <name of disk-defrag> 可打印磁盘信息，例如super_block，每个文件包含的block编号。
